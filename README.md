# HWL LLM Demo (Next.js, Bun, Supabase, Vercell AI-SDK)

First, add your environment variables to `.env` in the root of the project.

Second, run the development server using bun

```bash
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
